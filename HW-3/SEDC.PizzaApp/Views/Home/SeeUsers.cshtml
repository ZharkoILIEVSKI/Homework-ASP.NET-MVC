@using SEDC.PizzaApp.Models.Domain

@model List<User>

<h2>Users: </h2>

<ul>
    @for (int i = 0; i < Model.Count; i++)
    {
        <li>@Model.ElementAt(i).FirstName + @Model.ElementAt(i).LastName</li>
    }
</ul>
